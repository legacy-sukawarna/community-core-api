# ===========================================
# Environment Configuration
# ===========================================
# Copy this file to .env and fill in the values
# cp .env.example .env

# ===========================================
# Application
# ===========================================
NODE_ENV=development
PORT=3001

# ===========================================
# Database (Prisma + Supabase PostgreSQL)
# ===========================================
# Connection pooling URL (for serverless/edge)
DATABASE_URL=postgresql://postgres.[project-ref]:[password]@aws-0-[region].pooler.supabase.com:6543/postgres?pgbouncer=true

# Direct connection URL (for migrations)
DIRECT_URL=postgresql://postgres.[project-ref]:[password]@aws-0-[region].pooler.supabase.com:5432/postgres

# ===========================================
# Supabase
# ===========================================
SUPABASE_URL=https://[project-ref].supabase.co
SUPABASE_SERVICE_ROLE_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...

# Auth redirect URL (frontend callback)
REDIRECT_URL=http://localhost:3000/auth/callback

# ===========================================
# Email (Nodemailer SMTP)
# ===========================================
# SMTP server configuration
# Gmail: smtp.gmail.com (port 587)
# Outlook: smtp.office365.com (port 587)
# Mailtrap: sandbox.smtp.mailtrap.io (port 587)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_SECURE=false

# SMTP authentication
# For Gmail, use an App Password: https://myaccount.google.com/apppasswords
SMTP_USER=your-email@gmail.com
SMTP_PASS=your-app-password

# Sender email address
EMAIL_FROM="Your App Name <your-email@gmail.com>"

# ===========================================
# Form Webhook (Google Forms Integration)
# ===========================================
# API key for securing the webhook endpoint (optional but recommended)
# Generate a random string: openssl rand -hex 32
FORM_WEBHOOK_API_KEY=your-secret-api-key

# Admin emails to notify on form submission (comma-separated, optional)
ADMIN_NOTIFICATION_EMAILS=admin1@example.com,admin2@example.com

# ===========================================
# Error Tracking (Sentry)
# ===========================================
# Get your DSN from: https://sentry.io
SENTRY_DSN=https://xxxxx@xxxxx.ingest.sentry.io/xxxxx
